import { MentorSearchResult } from './mentor-search-result';

describe('MentorSearchResult', () => {
  it('should create an instance', () => {
    expect(new MentorSearchResult()).toBeTruthy();
  });
});
